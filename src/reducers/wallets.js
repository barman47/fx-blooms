const initialState = {
    wallets: []
};

const walletsReducer = (state = initialState, action) => {
    switch (action.type) {      

        default:
            return state;
    }
};

export default walletsReducer;