const initialState = {
    accounts: []
};

const bankAccountsReducer = (state = initialState, action) => {
    switch (action.type) {      

        default:
            return state;
    }
};

export default bankAccountsReducer;