import { Typography } from '@material-ui/core';

const Create = () => {
    return (
        <section>
            <Typography variant="h4">Make a Listing</Typography>
            <Typography variant="h4">Complete the form below to post a listing</Typography>
        </section>
    );
};

export default Create;